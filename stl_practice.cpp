#include<bits/stdc++.h>

using namespace std;

int main()
{
/*  list<int> l1;

    pair<int, string> my_pair, m2;
    my_pair = make_pair(1, "Hello");
    m2 = make_pair(2, "bye");
    cout << my_pair.first << " " << my_pair.second << endl;
    vector<int> myvect{1,2,3};
    vector<int> :: iterator i = myvect.begin();
    myvect.erase(i);
    myvect.resize(2, 5);
    for(i; i != myvect.end(); i++)
    cout << *i << " ";
    myvect.clear();
    cout << myvect.size() << " ";
    cout << myvect.capacity() << " ";
    myvect.push_back(2);
    cout << myvect.empty() << " ";
    cout << myvect.capacity();
    cout << myvect.size() << " ";*/
    return 0;
}
